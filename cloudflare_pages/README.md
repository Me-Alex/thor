# Thor on Cloudflare

## Deploy
1. Install Node.js
2. Run `npm install` inside `cloudflare_pages`
3. Set `API_BASE_URL` in Cloudflare Pages env vars
4. Run `npm run deploy`

## Endpoints
- `/api/health`
- `/api/listings`
